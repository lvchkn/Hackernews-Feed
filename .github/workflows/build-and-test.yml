name: build-and-test
on: push

jobs:
  build: 
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out the repo
      uses: actions/checkout@v6
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build --no-restore --configuration Release
      
    - name: Test
      env:
        GithubAuth:AppName: ${{ secrets.GH_AUTH_TEST_APP_NAME }}
        GithubAuth:ClientId: ${{ secrets.GH_AUTH_TEST_CLIENT_ID }}
        GithubAuth:ClientSecret: ${{ secrets.GH_AUTH_TEST_CLIENT_SECRET }}
        ClientUrl: ${{ secrets.CLIENT_URL }}
      run: dotnet test --no-build --configuration Release --verbosity normal
        
#        env:
#          GITHUBAUTH__APPNAME: ${{ secrets.GH_AUTH_TEST_APP_NAME }}
#          GITHUBAUTH__CLIENTID: ${{ secrets.GH_AUTH_TEST_CLIENT_ID }}
#          GITHUBAUTH__CLIENTSECRET: ${{ secrets.GH_AUTH_TEST_CLIENT_SECRET }}
#          CLIENTURL: ${{ secrets.CLIENT_URL }}